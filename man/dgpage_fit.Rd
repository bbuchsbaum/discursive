% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/dpage.R
\name{dgpage_fit}
\alias{dgpage_fit}
\title{Fit the DGPAGE model (Algorithm 1, Eqs. (12), (18), (23) in the paper)}
\usage{
dgpage_fit(
  X,
  S,
  D,
  r = 10,
  alpha = 0.001,
  beta = 1e-05,
  maxiter = 20,
  tol = 1e-05,
  verbose = TRUE,
  seed = NULL
)
}
\arguments{
\item{X}{A numeric matrix of size \eqn{n \times d} (training data).
\strong{Note}: We internally transpose \code{X} to follow the paper's notation.}

\item{S}{The \eqn{n x n} similarity graph (from \code{\link{compute_similarity_graph}}).}

\item{D}{The \eqn{n x n} diversity graph (from \code{\link{compute_diversity_graph}}).}

\item{r}{Target dimension (number of embedding directions to learn).}

\item{alpha}{The hyper-parameter in Eq. (12). Typical range: \eqn{[1e-6, 1e-1]}.}

\item{beta}{The hyper-parameter in Eq. (12). Typical range: \eqn{[1e-6, 1e-1]}.}

\item{maxiter}{Maximum number of iterations for the alternating updates.}

\item{tol}{Convergence tolerance on the objective value.}

\item{verbose}{Whether to print progress information.}
\item{seed}{Optional integer seed controlling the random initialization.}
}
\value{
A list with:
\itemize{
\item \code{P}: the \eqn{d \times r} projection matrix,
\item \code{W}: the \eqn{n \times n} learned adjacency matrix (adaptive graph),
\item \code{objective}: the final objective value,
\item \code{iter}: number of iterations taken,
\item \code{Z}: stored internally (\eqn{d x n}) = \code{t(X)} (just for reference).
}
}
\description{
This function implements the main training procedure of DGPAGE, which jointly learns
the adaptive graph \eqn{W} and the projection matrix \eqn{P} by minimizing
Eq. (12) in an alternating fashion.
}
\details{
\strong{Steps}:
\enumerate{
\item Initialize \eqn{P} randomly (size \eqn{d \times r}).
\item For each iteration:
\itemize{
\item Update \eqn{W} via Eq. (18) (closed-form).
\item Update \eqn{P} by solving the generalized eigenvalue problem in Eq. (23).
}
\item Repeat until convergence or \code{maxiter} is reached.
}

\strong{Dimensions} (in internal notation):
\itemize{
\item Let \eqn{Z = t(X)} be \eqn{d x n},
\item \eqn{P} is \eqn{d x r},
\item \eqn{Y = P^\top Z} is \eqn{r x n}.
\item \eqn{W} is \eqn{n x n}.
\item \eqn{S}, \eqn{D} are \eqn{n x n}.
}
}
